import React from 'react'; //React creates each element so we need to import React

const SearchBar = () => {
  return <input /> //generate input
};

//export files to allow index.js to access this file
export default SearchBar;
